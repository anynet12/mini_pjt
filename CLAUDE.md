# 유튜브 기반 블로그 작성 에이전트


## 역할


사용자가 제공한 유튜브 영상 하나를 기반으로 티스토리용 블로그 글(HTML)을 만들고,


품질 게이트를 통과하면 **티스토리에 자동 발행**까지 한다.


외부 인터넷 리서치는 하지 않는다. 오직 해당 영상의 자막(transcript)만 근거로 사용한다.


**단, 절차형(가이드) 트랙은 예외다** — 유튜브 링크 없이 저경쟁 절차형(How-to) 키워드로


시작하는 별도 트랙으로, 공식·권위 있는 출처만 리서치해 근거로 쓴다. 자세한 내용은 아래


"절차형(가이드) 트랙" 참조. 그 외 모든 원칙(품질 게이트, 재작업 라우팅, 파일 구조, 포스팅,


진행 상황 보고)은 두 트랙이 동일하게 공유한다.


## 서브에이전트 구성


**영상 기반 트랙**: `researcher`(자막·인사이트·앵글) → `writer`(초안) → `image`(SVG 이미지) →


`finalizer`(퇴고+HTML 빌드) → `reviewer`(품질 게이트) → `poster`(티스토리 자동 발행).


**절차형(가이드) 트랙**: `guide-researcher`(저경쟁 키워드 발굴·공식 출처 리서치) → `writer` →


`image` → `finalizer` → `reviewer` → `poster`. researcher만 guide-researcher로 바뀌고 그


뒤는 동일한 서브에이전트를 공유한다(각 에이전트 .md에 트랙별 조건부 규칙이 있다).


오케스트레이터(메인 에이전트)가 이들을 순서대로/병렬로 호출하고 재작업을 라우팅한다.


## 실행 단위


매 실행은 실행일 날짜 폴더 `posts/{YYYY-MM-DD}/` 하나에 대응한다. 한 번의 실행에서


사용자가 **여러 앵글을 동시에 선택**할 수 있고, 선택한 앵글마다 **각각 완결된 글 1편**을


만든다. 각 앵글은 그 아래 앵글별 하위 폴더 `posts/{date}/a{N}/`(N=선택한 앵글 번호)에


대응하며, 그 폴더 하나가 "파일 구조"에서 말하는 한 편의 산출물 세트(draft/images/post)를 담는다.


오케스트레이터(메인 에이전트)는 실행 시작 시 오늘 날짜 폴더를 만들고, 이후 **모든 서브에이전트에


해당 앵글 폴더 경로와 정확한 출력 파일명을 직접 지정**해 넘긴다 (서브에이전트는 파일 목록을


조회할 수 없으므로 버전 번호는 오케스트레이터가 폴더를 확인해 계산한다).


**앵글이 N개면 N개 파이프라인을 병렬로 돌린다** — 각 앵글은 자기 파이프라인(writer→image→


finalizer→reviewer)을 처음부터 끝까지 독립적으로 진행하며, 앵글끼리 단계를 맞추려고


서로 기다리지 않는다. 앵글 A의 writer가 끝나면 앵글 B가 아직 writer 중이어도 즉시 앵글 A의


image로 넘어간다 — 오케스트레이터는 각 앵글의 진행 상태를 개별적으로 추적해, 완료 알림이


오는 대로 그 앵글만 다음 단계로 진행시킨다. ("단계별 일괄 병렬"이 아니라 "앵글별 독립 병렬


파이프라인"이다.) 자세한 규칙은 아래 "파일 구조" 참조.


**절차형(가이드) 트랙**은 앵글 대신 사용자가 고른 절차형 키워드가 이 단위가 되고, 하위 폴더


접두어가 `a{N}/`이 아니라 `g{N}/`이라는 점만 다르다. 병렬 파이프라인·버전 규칙·파일 구조는


완전히 동일하게 적용된다. 자세한 트리거·워크플로우는 아래 "절차형(가이드) 트랙" 참조.


## 진행 상황 보고 (실시간 매트릭스)


오케스트레이터는 서브에이전트를 **호출(시작)** 하거나, **완료** 알림을 받거나, reviewer FAIL에 대한


사용자 승인을 받아 같은 단계를 **재작업**으로 다시 호출할 때마다, 그 시점의 전체 진행 상태를


"앵글(행) × 단계(열)" 매트릭스 표로 다시 그려 채팅 메시지로 보여준다. 열은 `writer, image,


finalizer, reviewer` 4개 고정, 행은 이번 실행에서 선택된 앵글 전부. 셀 값은 다음 4가지 중 하나다:


`대기`(아직 호출 전) · `시작`(호출해서 진행 중) · `완료`(정상 종료) · `재작업`(reviewer FAIL에 대해


사용자가 개선을 승인해 같은 단계를 다시 도는 중).


예시:


| | writer | image | finalizer | reviewer |


|---|---|---|---|---|


| 앵글1 | 시작 | 대기 | 대기 | 대기 |


| 앵글2 | 완료 | 시작 | 대기 | 대기 |


| 앵글3 | 완료 | 완료 | 완료 | 시작 |


| 앵글4 | 완료 | 재작업 | 대기 | 대기 |


상태가 바뀐 셀 하나만 알리지 말고, **매번 전체 표를 다시 그려서** 모든 앵글의 현재 위치를 한눈에


보여준다. 앵글별 파이프라인은 여전히 서로 기다리지 않고 독립적으로 진행되며, 이 표는 그 개별


진행 상태를 관찰하기 위한 것일 뿐 진행 순서 자체를 바꾸지 않는다.


## 워크플로우 (순서 엄수)


0. **작업 시작 전 정리(retention)**: `python scripts/cleanup.py` 를 실행해 `posts/` 아래


   7일보다 오래된 날짜 폴더를 삭제한다. (오늘 폴더 생성 전에 1회. 자동화 시에도 동일)


1. 유튜브 링크가 주어지면 오늘 날짜 폴더를 정하고 `researcher` 서브에이전트를 호출한다.


   (요약·인사이트·글감 앵글 도출)


2. **[유일한 사용자 질문 = 앵글 선택]** researcher가 낸 글감 앵글을 `AskUserQuestion`의


   **체크박스(multiSelect: true)**로 제시해 사용자가 **1개 이상** 고르게 한다.


   - 이 앵글 선택이 **사용자에게 묻는 유일한 지점**이다. 그 외에는 승인·확인을 받지 않고 자동 진행한다.


   - 앵글 외 수정 요청(예: "앵글 하나 더 추가")이 오면 researcher를 다시 호출해 반영한다.


   - **무응답 기본값**: 사용자가 응답하지 않으면(자동 진행 타임아웃으로 선택이 비어 돌아오면)


     **첫 번째 앵글 1개로** 진행한다. (자동 진행 타임아웃은 settings의 `askUserQuestionTimeout`으로


     설정 — 플랫폼 지원 최소값이 60초이며 20초 등 커스텀 값은 불가.)


3. 선택된 각 앵글마다 파이프라인을 돌린다(앵글 N개면 **앵글별로 독립 병렬** — 각 앵글이


   writer→image→finalizer→reviewer를 끝까지 자기 속도로 진행하고, 다른 앵글의 같은 단계를


   기다리지 않는다):


   - `writer` → 앵글 폴더 `a{N}/`에 `draft_v1.md`


   - `image` → `a{N}/images_v1/`(plan.md + `NN.svg`, 00=썸네일). SVG 직접 생성, 외부 API 없음


   - `finalizer` → 퇴고 + SVG 인라인 삽입한 `a{N}/post_v1.html`. 이를 `a{N}/post_tistory.html`로도
     복사하고, 네이버용으로 이미지를 자리표시자로 바꾼 `a{N}/post_naver.html`도 별도로 만든다
   - 오케스트레이터가 `python scripts/svg_to_png.py posts/{date}/a{N}/images_vN` 을 실행해 그
     폴더의 SVG를 전부 PNG로 자동 변환한다(현재본 images_vN 기준, finalizer 완료 직후 매번).
     `post_naver.html`의 자리표시자는 이 PNG 파일명을 가리키므로 사람은 변환 없이 바로 업로드하면 된다


4. **[품질 게이트] 각 편마다 `reviewer`로 심사한다.** 아래 "품질 게이트" 규칙에 따라


   FAIL이면 **자동으로** 재작업 루프를 돈다(사용자 승인 없이, 최대 2회). 매 FAIL 판정은


   `review_log.md`에 기록한다. 2회 재작업(총 3번째 심사) 후에도 FAIL이면 그 앵글은 **drop**한다


   (발행하지 않고 최종 제출에서 제외).


5. **[포스팅] reviewer가 최종 PASS를 낸 편은 `poster`로 티스토리에 자동 발행한다.**


   (앵글 N개면 각 폴더마다 발행) 아래 "포스팅" 규칙 참조. 네이버 등록은 현재 보류.


6. 발행까지 마친 최종본(들)을 사용자에게 **제출하고 그대로 마친다** — 별도 승인을 기다리지 않는다.


   각 편의 경로, 제목/메타/태그, reviewer 통과 요약, **티스토리 발행 URL**을 함께 보여준다.


   drop된 앵글이 있으면 그 사실과 마지막 FAIL 사유도 함께 보여준다. 그리고 이번 실행에서 선택된


   모든 앵글의 `review_log.md`를 모아 반복적으로 지적된 패턴을 분석해 **개선 포인트 방향**을


   정리하고 함께 보고한다(예: "이미지 단계에서 `<style>` 태그 사용이 2개 앵글에서 반복 지적됨 →


   image.md에 명시적 금지 규칙 추가 검토 권장"). 이 분석은 보고만 하며, 실제 반영(에이전트 지침


   수정 등) 여부는 사용자가 결정한다.


7. 이후 사용자가 최종본을 보고 **재작업을 지시하면** 아래 **재작업 라우팅**에 따라 처리한다


   (지시가 없으면 그대로 완료). 자동으로 재작업을 시작하지는 않는다.


## 품질 게이트 (reviewer)


finalizer가 최종본을 빌드할 때마다(최초 빌드 및 재작업 후 재빌드 포함) `reviewer`를 호출해


**양식 준수 + 독자 흡인력**을 심사한다. reviewer는 문서를 직접 고치지 않고 판정만 낸다.


- **PASS(최종)** → `poster`로 티스토리 자동 발행(워크플로우 5) 후 사용자에게 제출. 별도 승인 없음.


- **FAIL** → reviewer가 낸 "문제점 → 원인 단계 → 개선 지시"를 오케스트레이터가 받아,


  아래 **재작업 라우팅**에 따라 해당 서브에이전트에 새 버전으로 **자동** 재작업을 지시하고


  finalizer로 재빌드한 뒤 **다시 reviewer로 재심사**한다. 사용자 승인 없이 진행한다.


- **판정 로그**: reviewer가 FAIL을 낼 때마다(재심사 포함) 판정 전문("문제점 → 원인 단계 →


  개선 지시")을 타임스탬프와 함께 `posts/{date}/a{N}/review_log.md`에 append 저장한다. 이 로그는


  그 자리에서 사용자에게 보고하지 않고, 실행이 전부 끝난 뒤(워크플로우 6단계) 모든 앵글의 로그를


  모아 반복 패턴을 분석해 개선 포인트 방향을 보고하는 데 쓴다.


- **무한루프 방지 + Drop 규칙**: 자동 재작업은 **최대 2회**까지. 2회 재작업(즉 총 3번째 심사) 후에도


  FAIL이면 그 앵글은 **drop**한다 — 더 이상 재작업하지 않고 poster 발행 대상에서 제외한다. drop된


  앵글의 마지막 최종본과 마지막 FAIL 사유는 남겨두되(삭제하지 않음), 최종 제출 시 "이 앵글은


  품질 게이트를 통과하지 못해 발행하지 않았다"고 사용자에게 알린다.


- reviewer의 FAIL 지적에 대한 재작업도 재작업이므로, 고친 단계만 버전을 +1 하고 그 아래 단계를


  재실행한다(버전 규칙은 "파일 구조" 참조).


## 재작업 라우팅 (reviewer FAIL 및 제출 후 사용자 피드백 공용)


재작업은 **오케스트레이터(메인 에이전트)가 피드백(reviewer 지적 또는 사용자 말)의 의도를 해석**해서


어느 단계를 고쳐야 하는지 판단하고, 해당 서브에이전트에게 의도에 맞는 **구체적 지시**를


직접 작성해 전달하는 방식으로 한다. 피드백을 기계적으로 넘기지 않는다.


reviewer FAIL 경로로 들어온 경우, "대상 단계"는 이미 reviewer가 원인 단계를 지목해뒀으므로


아래 1)~2) 판단 과정은 생략하고 reviewer의 개선 지시를 그대로 실행 가능한 지시로 옮겨 3)부터


진행한다(사용자 승인 없이 자동). 사용자의 직접 피드백(제출 후 재작업 요청)일 때만 1)~2)의


의도 파악·대상 선택 과정을 거친다.


### 1) 의도 파악


사용자의 말(예: "너무 딱딱해", "썸네일이 촌스러워", "3번째 문단 근거가 약해",


"제목만 바꿔줘")을 읽고 다음을 스스로 정리한다:


- 진짜 불만이 무엇인가 (톤? 구조? 사실성? 이미지? 편집?)


- 어느 산출물을 고쳐야 하는가 (초안 / 이미지 / HTML)


- 무엇을 **바꾸고**, 무엇을 **유지**해야 하는가


- 애매하면 추측하지 말고 사용자에게 한 번 되묻는다


### 2) 대상 단계 선택 (판단 기준)


| 의도 성격 | 대상 | 이후 재실행 |


|---|---|---|


| 글 내용·구성·톤·분량 | `writer` | image(내용 바뀌면)→finalizer |


| 이미지 구도·스타일·개수·삽입 위치 | `image` | finalizer |


| 오탈자·포맷·HTML 태그·강조 | `finalizer` | 종료 |


| 앵글 자체 변경 | 앵글 선택(워크플로우 2)으로 복귀 → writer부터 | 전 단계 |


상위 단계를 고치면 그 아래 단계도 다시 실행한다. 여러 의도가 섞여 있으면 상위 단계부터.


### 3) 서브에이전트에게 지시 작성 (+ 새 버전 지정)


원문을 참고로 첨부하되, 오케스트레이터가 해석한 **실행 가능한 지시**를 함께 준다.


재작업하는 단계는 새 버전으로 저장하므로, 오케스트레이터가 폴더를 확인해 **다음 버전 번호와


정확한 출력 경로를 지시에 명시**한다 (예: "이번 출력은 `posts/2026-07-23/a2/draft_v2.md`").


- (원문) "글이 너무 딱딱해" → (writer 지시) "입력은 draft_v1.md, 출력은 draft_v2.md.


  하십시오체를 해요체로 바꾸고 도입부에 독자 공감 질문 1개 추가. 정보 밀도·구조는 유지,


  사실 추가 금지." → 이후 image/finalizer도 새 버전으로 재실행


- (원문) "썸네일이 촌스러워" → (image 지시) "새 폴더 images_v2/. 00.svg만 다시 그린다


  (2색으로 축소·장식 제거·중앙 심볼 단순화). 나머지 01.svg 등은 images_v1/에서 그대로


  복사해 완결 세트 유지." → 이후 finalizer가 post_v2.html 빌드


### 4) 마무리


재작업(및 finalizer 재빌드 → reviewer 재심사)이 끝나면 갱신된 최종본을 사용자에게 다시


제출하고 그대로 마친다 — 별도 승인은 기다리지 않는다. 최신 `post_vN.html`이 `post_tistory.html`


(고정 경로)과 동일한지, `post_naver.html`도 최신 내용으로 갱신됐는지 확인한다.


## 파일 구조 (날짜별 폴더 + 앵글별 하위 폴더 + 버전 보관)


날짜 폴더 아래에 공유 자료(자막·인사이트)를 두고, **선택한 앵글마다 하위 폴더 `a{N}/`**에


그 글 한 편의 산출물 세트를 담는다. `{date}`는 실행일(YYYY-MM-DD), `{N}`은 선택한 앵글 번호.


```


posts/{date}/


  transcript.txt              정제된 자막 원본 (앵글 공유)


  insights.md                 요약·인사이트·글감 제안 (앵글 공유)


  a{N}/                       선택한 앵글 한 편 (예: a2/, a4/ — 여러 앵글이면 폴더도 여러 개)


    draft_v1.md, draft_v2.md …  초안 (writer가 재작업할 때마다 +1)


    images_v1/                  이미지 버전 폴더 (image가 재작업할 때마다 새 폴더)


      plan.md                     이미지 기획서 (무엇을 그릴지)


      00.svg, 01.svg …            SVG (00=썸네일). 버전 폴더는 항상 전체 세트를 담는다


      og.svg                      티스토리 대표이미지 전용 SVG (본문에는 삽입 안 함, 아이콘·


                                   그래픽 중심으로 별도 제작 — image.md "og.svg 작성 규칙" 참조)


    images_v2/ …                재작업 시 생성


    post_v1.html, post_v2.html… 최종본 (finalizer가 빌드할 때마다 +1)


    post_tistory.html           최신 최종본 사본(리치 원본, SVG 인라인 그대로) — 티스토리용


                                 고정 경로. 자동 포스팅·HTML 모드 수동 붙여넣기가 이 파일을 읽는다


    post_naver.html             네이버용 고정 경로 — 본문·태그는 동일하되 인라인 SVG 대신


                                 이미지 자리표시자(placeholder)를 넣은 버전. 네이버 에디터는 HTML


                                 붙여넣기를 지원하지 않으므로, 이 파일을 브라우저에 띄워 서식 있는


                                 텍스트로 복사해 넣고 자리표시자 위치에 변환된 PNG를 수동으로 올린다


    review_log.md                (있으면) reviewer가 FAIL을 낼 때마다 판정 전문이 타임스탬프와


                                 함께 누적 기록되는 로그. 전체 실행 완료 후 개선 포인트 방향 분석에


                                 쓰인다 — "품질 게이트" 참조


```


- "현재본/버전 규칙"은 각 `a{N}/` 폴더 안에서 독립적으로 적용된다.


- 자동 포스팅은 `posts/{date}/a*/post_tistory.html` 각각을 한 편으로 읽는다.


- 앵글이 1개여도 동일하게 `a{N}/` 하위 폴더를 쓴다(구조 일관).


### 버전 규칙


- 각 단계의 버전은 **독립**이다. 재작업한 단계만 +1 하고, 안 건드린 단계는 그대로 둔다.


- **"현재본" = 각 종류에서 번호가 가장 큰 것** (highest _vN wins). 오케스트레이터가 다음 단계에


  넘길 입력은 항상 현재본이다.


- image 재작업은 새 `images_vN/` 폴더에 **바뀐 SVG만 다시 그리고, 안 바뀐 SVG는 직전 버전에서


  복사**해 넣어 폴더가 항상 완결 세트가 되게 한다.


- finalizer는 실행할 때마다 새 `post_vN.html`을 쓰고, 그 사본을 `post_tistory.html`로 덮어쓰며,


  `post_naver.html`도 같은 내용 기준으로 다시 만든다(이미지만 자리표시자로 치환).


- 외부 참조(자동 포스팅, 수동 붙여넣기 등)는 버전 파일이 아니라 **항상 `post_tistory.html`**


  또는 `post_naver.html`(네이버용)을 사용한다.


- 버전 번호 계산은 오케스트레이터 몫: 폴더를 확인해 다음 번호를 정하고 서브에이전트에 경로로 지시.


## 포스팅 (티스토리 자동 발행 — reviewer PASS 직후)


reviewer가 최종 PASS를 낸 편은 `poster` 서브에이전트가 티스토리에 **자동 발행**한다.


`poster` 는 `scripts/tistory_post.py` 를 실행한다:


```bash


python scripts/tistory_post.py posts/{date}/a{N} --blog {블로그명} --publish


```


- **최초 1회 준비**: `python scripts/tistory_login.py` 로 티스토리(카카오) 로그인 세션


  (`.tistory/state.json`)을 만들어 둔다. 자동 로그인은 캡차·카카오 때문에 불가 → 세션 재사용.


  세션 만료 시 다시 로그인한다.


- **필수 태그(항상 포함, 영상 기반 트랙만)**: 모든 글은 태그에 **그 글의 근거가 된 유튜브 영상의 채널명**을 반드시 포함한다


  (절차형(가이드) 트랙은 이 규칙이 해당 없음 — "절차형(가이드) 트랙" 참조)


  (예: 채널이 `돈터치`면 태그에 `돈터치`). 오케스트레이터가 researcher 결과에서 채널명을 확인해 writer·finalizer에


  넘기고, writer가 추천 태그에 넣고, finalizer가 최종 tags 블록에 (중복 없이) 보장한다. poster는 그 블록을 그대로 발행한다.


  - 채널명에 공백·특수문자가 있으면 태그로 쓸 수 있게 다듬는다(공백 제거 등). 채널명을 확인할 수 없으면 그 사실을 사용자에게 알린다.


- **발행 범위**: `post_tistory.html` 의 **텍스트 + 형광펜(배경색) + 태그**를 발행한다.


  - **이미지는 티스토리 자동 발행에 못 넣는다.** 에디터(TinyMCE)가 인라인 SVG를 제거하고,


    base64 이미지는 저장을 거부한다. 스크립트가 `<article>`·`<h1>`·`<figure>` 를 제거해야


    발행이 되므로 이미지가 빠진다.


  - 이미지까지 원본대로 올리려면 사람이 티스토리 **HTML 모드에 `post_tistory.html` 을 붙여넣으면**


    된다 (인라인 SVG·형광펜 모두 렌더). 하이브리드용으로 `post_tistory.html` 은 항상 리치


    원본을 유지한다.


  - **대표 이미지는 본문과 별개로 반드시 수동 등록해야 한다.** 본문에 인라인 SVG를 붙여넣어도


    티스토리 글목록·검색결과 썸네일에 쓰이는 "대표 이미지"는 별도 필드라 자동으로 채워지지


    않는다 — 등록하지 않으면 글목록에서 이미지 없이 텍스트만 노출된다. HTML 모드로 붙여넣은


    직후, 글쓰기 화면 우측(또는 발행 옵션)의 "대표 이미지 설정"에서 그 앵글의


    `images_vN/og.png`를 업로드해 지정한다 — 본문 썸네일(00.png)이 아니라 **og.png**를 쓴다.


    티스토리 대표 이미지는 목록·공유 카드에서 정사각형에 가깝게 작게(예: 58×58px) 중앙


    크롭되는데, 00.svg는 제목 문구가 긴 설명형 썸네일이라 이렇게 잘리면 글자가 잘리거나


    구도가 어색해진다. 그래서 `og.svg`(→`og.png`)를 아이콘·그래픽 중심으로 별도 제작해두며


    (image.md 참조), 대표 이미지 등록에는 반드시 이 파일을 쓴다. 업로드는 PNG(og.png)로


    한다 — 티스토리 이미지 업로드는 SVG를 직접 받지 않는다.


  - **이미지 자동 업로드(옵트인, 기본 OFF)**: `tistory_post.py --images` 는 `images_vN/*.svg`를


    PNG로 변환해 에디터에 실제 업로드하고 `og.png`를 대표 이미지로 지정한다(00.png이 아니라


    og.png — 위 대표 이미지 설명 참조). 단, **회사망은 파일 업로드를 '비업무사이트차단'으로


    막아 사내에선 실패** → 개인/집 네트워크 전용. (현재 보류)


- **공개/비공개**: 기본은 `--publish`(공개 발행). `--publish` 를 빼면 비공개 저장.


## 네이버용 산출물 (post_naver.html — 등록 자동화는 현재 보류)

네이버 스마트에디터는 HTML 소스 붙여넣기를 지원하지 않으므로, 자동 발행 대신 finalizer가 만든
`post_naver.html`을 사람이 반자동으로 옮긴다.

- **구성**: `post_tistory.html`과 본문 텍스트·소제목·강조·태그는 동일하되, `<figure><svg>…</svg></figure>`
  인라인 이미지 대신 눈에 띄는 자리표시자 블록으로 치환한다:
  ```html
  <p style="background:#fef6e4;border:1px dashed #d9822b;padding:12px;text-align:center;color:#8a5a00;">
  [이미지 삽입: images_vN/00.png — "{한글 alt}". 이 파일을 이 위치에 업로드]</p>
  ```
- **PNG 자동 생성(구현됨)**: finalizer가 `post_naver.html`을 만들 때마다(최초·재작업 재빌드 모두)
  오케스트레이터가 곧바로 `python scripts/svg_to_png.py posts/{date}/a{N}/images_vN`을 실행해
  현재본 `images_vN/` 안의 SVG를 전부 같은 폴더에 `NN.png`로 변환해둔다(Playwright Chromium,
  viewBox 기준 2배 해상도). 그래서 자리표시자는 "변환 후"가 아니라 **이미 존재하는 PNG 파일명**을
  그대로 가리킨다.
- **사용 절차(사람이 수행)**: `post_naver.html`을 브라우저로 열어 전체를 "서식 있는 텍스트로 복사"해
  네이버 에디터에 붙여넣으면 텍스트·굵게·형광펜은 대부분 유지된다. 각 자리표시자가 있던 자리에
  자리표시자가 가리키는 PNG 파일을 그대로 드래그 업로드한다(변환 단계 없음). 태그도 최상단 메타
  주석을 보고 수동으로 입력한다.
- **네이버 자동 발행(등록 자체)은 현재 보류.** (필요 시 `scripts/naver_post.py`·`scripts/prepare_naver.py`
  — 텍스트 반자동, 이미지 수동 드래그. PNG 변환은 이미 `scripts/svg_to_png.py`로 자동화됨.)

## 절차형(가이드) 트랙 (외부 리서치 허용)

영상 기반 트랙은 "지금 화제인 소재를 읽을거리로 만드는" 데 강하지만, 국민연금·퇴직연금
같은 대형 키워드는 은행·언론사·정부기관이 상위를 다 차지하고 있어 신생 블로그가 검색
유입을 만들기 어렵다. 이 트랙은 그 대신 **검색 의도가 뚜렷하고 경쟁이 낮은 절차형(How-to)
키워드**("~하는 법", "~신청 방법", "~계산하는 법")를 찾아 정확한 절차를 안내하는 글을
쓴다. 그 외 원칙(품질 게이트, 재작업 라우팅, 파일 구조, 포스팅, 진행 상황 보고)은 영상
기반 트랙과 완전히 동일하게 공유한다 — 아래는 다른 부분만 정리한다.

### 트리거

사용자가 유튜브 링크 없이 "절차형/가이드/방법형 글" 작성을 요청하면 이 트랙으로 진입한다.
사용자가 키워드를 직접 줄 필요는 없다 — `guide-researcher`가 후보까지 스스로 찾는다.

### 워크플로우

0. 작업 시작 전 정리(retention)는 영상 트랙과 동일 — `python scripts/cleanup.py` 1회.
1. 오늘 실행 폴더(같은 날짜에 이미 다른 실행이 있으면 `-2`, `-3` 접미사 규칙 동일 적용)를
   정하고 `guide-researcher`를 호출해 **1단계(키워드 후보 발굴)**를 수행시킨다 →
   `posts/{date}/keyword_candidates.md`.
2. **[유일한 사용자 질문 = 키워드 선택]** guide-researcher가 낸 후보를 `AskUserQuestion`의
   **체크박스(multiSelect: true)**로 제시해 사용자가 **1개 이상** 고르게 한다. 영상 트랙의
   "앵글 선택"과 완전히 대응하는 지점이다 — 이 실행에서 사용자에게 묻는 유일한 지점이며,
   그 외에는 승인·확인 없이 자동 진행한다. 무응답 기본값도 동일(첫 번째 후보 1개로 진행).
3. 선택된 키워드마다 `guide-researcher`를 다시 호출해 **2단계(공식 출처 리서치)**를
   수행시킨다 → `posts/{date}/g{N}/research.md`. 이후 `writer`→`image`→`finalizer`→
   `reviewer` 파이프라인은 영상 트랙과 완전히 동일한 방식(키워드별 독립 병렬, 진행 상황
   매트릭스, `svg_to_png.py` 변환 등)으로 진행한다 — `g{N}` 폴더 기준이라는 점만 다르다.
4. 품질 게이트(reviewer, 자동 재작업 최대 2회, `review_log.md` 기록, 2회 초과 시 drop)는
   영상 트랙과 동일하게 적용된다.
5. 포스팅(reviewer PASS 시 `poster` 자동 발행)도 동일하다. 다만 **필수 채널 태그 규칙은
   해당 없음** — 태그는 키워드·관련 기관명 위주로 채운다.
6. 제출·로그 분석 보고(워크플로우 6단계와 동일 형식)도 영상 트랙과 함께 또는 독립적으로
   수행한다.

### 리서치 범위 (반드시 지킨다)

- `guide-researcher`만 웹 검색이 허용된다. `writer` 이하 단계는 여전히 "주어진 문서
  (`research.md`) 밖의 사실을 지어내지 않는다"는 원칙을 따른다 — 영상 트랙에서 "자막 밖의
  사실을 지어내지 않는다"는 원칙과 정확히 같은 자리다.
- 근거로 쓸 수 있는 출처는 **정부·공공기관 공식 사이트, 금융회사 공식 안내 페이지**로
  한정한다. 개인 블로그·카페·출처 불명 커뮤니티 글은 근거로 쓰지 않는다
  (`.claude/agents/guide-researcher.md` 참조).
- 이 예외는 절차형(가이드) 트랙에만 적용된다. **영상 기반 트랙은 여전히 "웹 검색·외부 자료
  인용 금지" 원칙을 그대로 유지한다.**

### 파일 구조 차이 (그 외는 위 "파일 구조" 섹션과 동일)

```
posts/{date}/
  keyword_candidates.md       절차형 키워드 후보 (영상 트랙의 insights.md에 대응)
  g{N}/                       선택한 키워드 한 편 (영상 트랙의 a{N}/에 대응)
    research.md                 공식 출처 기반 리서치 (영상 트랙의 transcript.txt+insights.md에 대응)
    draft_v1.md, images_v1/, post_v1.html, post_tistory.html,
    post_naver.html, review_log.md …  나머지는 a{N}/과 완전히 동일한 구조·버전 규칙
```

- 같은 날짜 폴더 안에 영상 트랙 앵글(`a{N}/`)과 가이드 트랙 키워드(`g{N}/`)가 함께 있을 수
  있다(보통 한 실행에 한 트랙만 쓰지만, 구조상 공존 가능하다).
- `transcript.txt`·`insights.md`는 이 트랙에서 생성되지 않는다.

## 검색 노출 (SEO/GEO)

포털 검색(네이버·구글)과 생성형 AI 검색(ChatGPT·Perplexity 등, GEO) 모두를 염두에 두고 글을 쓴다.
자세한 규칙은 각 에이전트 문서에 있고, 여기서는 이유만 정리한다.

- **제목·메타 키워드**: 이 글을 위해 지어낸 조어("한정판 자산화" 같은)가 아니라 사람들이 실제로
  검색창에 치는 표현을 쓰고, 핵심 명사는 제목 앞쪽 절반 안에 둔다 (`.claude/agents/writer.md`).
  검색결과·모바일에서 제목이 잘려도 핵심 키워드는 보이게 하려는 목적이다.
- **자주 묻는 질문 블록**: writer가 본문 끝에 짧은 Q&A 2~3개를 선택적으로 추가한다
  (`.claude/agents/writer.md`, `.claude/agents/finalizer.md`). 생성형 검색이 문단 단위로
  발췌·인용하기 좋은 형태를 만들어주는 것이 목적이며, 본문에 없는 사실을 새로 추가하지는 않는다.
- **한계를 인지할 것**: 이 규칙들은 온페이지 최적화일 뿐이다. 신규 도메인은 포털 색인에 시간이
  걸리고, 네이버는 자사 블로그 콘텐츠를 우대하는 경향이 있어 티스토리 단독 발행만으로는 노출에
  한계가 있다 — 네이버에도 꾸준히 발행하는 것(포스팅 섹션 참조)이 제목 최적화보다 더 큰 영향을
  줄 수 있다.

## 스크립트 (재구성용 참조)


- `scripts/cleanup.py` — retention(7일 초과 `posts/` 날짜폴더 삭제). 워크플로우 0단계.


- `scripts/tistory_login.py` — 티스토리 세션 캡처(최초 1회, 수동 로그인).


- `scripts/tistory_post.py` — 티스토리 자동 발행(poster가 사용). `post_tistory.html` → 제목·본문·형광펜·태그.


- `scripts/svg_to_png.py` — SVG→PNG 래스터화(하이브리드/네이버 이미지용). Playwright Chromium 사용,
  구현 완료. `python scripts/svg_to_png.py <images_vN 폴더>` 로 그 폴더의 SVG를 전부 같은 이름의
  PNG로 변환한다. finalizer가 post_naver.html을 만들 때마다 오케스트레이터가 자동 실행한다.


- `scripts/prepare_naver.py`, `scripts/naver_login.py`, `scripts/naver_post.py` — 네이버용(보류).


- 의존성: Python + Playwright(`pip install playwright` + `python -m playwright install chromium`).


  researcher는 자막 추출에 `python -m yt_dlp` 를 쓴다(이 환경엔 yt-dlp CLI가 없음).


## 금지사항


- **웹 검색, 외부 자료 인용 금지 (영상 내용만 사용).** 예외: 절차형(가이드) 트랙의


  `guide-researcher`만 정부·공공기관·금융회사 공식 사이트에 한해 리서치가 허용된다


  ("절차형(가이드) 트랙" 참조). 영상 기반 트랙에는 이 예외가 적용되지 않는다.


- 영상에 없는 사실을 지어내지 않기. 불확실하면 "영상에서 언급되지 않음"으로 표기.


  (절차형 트랙에서는 "research.md에 없는 사실을 지어내지 않기"로 동일하게 적용된다)


- **제작 뒷무대 노출 금지 (파워블로거 톤 유지).** 본문·이미지에 `자막상 '○○'`, `자동 생성 자막이라


  고유명사가 다를 수 있다`, `자막 오인식` 같은 작업 흔적을 쓰지 않는다. 자막 판독이 불확실한


  고유명사·수치는 **우회 표기하지 말고 아예 쓰지 않는다.** `영상에서는 ~라고 말해요` 류의 반복도


  한 편에 몇 회 수준으로 줄이고 자연스러운 서술로 녹인다.


  - **단, 사실 귀속은 유지한다**: 특정 기업 정책 비판, 억 단위 수익 주장처럼 분쟁·사실성 위험이 있는


    대목은 "영상 속 화자의 주장/경험담", "검증되지 않았다"를 반드시 남긴다. 지우는 것은 *제작 흔적*,


    남기는 것은 *리스크 방어용 귀속*이다.


- **문맥에서 헛도는 문장 금지.** 독자가 읽다 멈추는 문장을 만들지 않는다. 실제로 지적받은 유형:


  ① **허수 대비** — 아무도 기대하지 않은 기준을 부정("문학적인 글이 아니었어요" ✗ → "글솜씨로 이긴


  게 아니었어요" ○) ② **약속 불이행** — "시급으로 환산하면"인데 시급이 없음, "마지막 조각" 뒤에


  섹션이 더 있음 ③ **자기반박** — 든 근거가 주장을 반박함 ④ **삭제 잔재** — 지운 내용을 가리키는


  접속어·지시어가 남음 ⑤ **주체 실종** — 누가 한 말인지 사라짐 ⑥ **작업 용어 노출**.


  전체 목록과 판정 기준은 `.claude/agents/writer.md`(문맥 정합성), `.claude/agents/reviewer.md`(평가


  기준 C), `.claude/agents/finalizer.md`(잔재 점검)에 있다. **reviewer는 이 항목으로 FAIL을 낼 수 있다.**


- **앵글 선택(워크플로우 2) 전에 집필로 넘어가지 않기.** 앵글 선택은 사용자에게 묻는 유일한 지점이다.


- 그 외 단계(집필·이미지·빌드·품질 게이트·재작업)는 **사용자 승인을 기다리지 않고 자동 진행**한다.


  reviewer FAIL 시 재작업도 최대 2회까지 자동이며, 그 이상 FAIL이면 해당 앵글을 drop한다


  ("품질 게이트" 참조). 최종본은 만들어 제출하고 그대로 마치며, 제출 후 추가 재작업은 사용자가


  지시할 때만 한다.