# 기존 파이프라인
playwright
yt-dlp
# 패턴 1 (LCEL chain + Pydantic 구조화 출력) — Bedrock 경유
boto3
langchain-core
langchain-aws
pydantic>=2
python-dotenv
# 패턴 3 (RAG) — Chroma 벡터 저장소 + BM25 키워드 검색 (임베딩·리랭킹은 Bedrock)
chromadb
rank_bm25
# src/spike (SDK 실험)
claude-agent-sdk
